# Public experience audit — 2026-09-08

Verified core: 8c362e10fba65cbc2279f08c06eb5469e16a72e5.

Routes inspected: /, /examples, /benchmarks, /versions, /repositories, /repositories/:repository, fallback. No public /architecture or /about exists.

The previous home repeats eyebrow/heading/copy/cards, uses generic CPU/box icons instead of ownership diagrams, and leaves iOS outside the hero output. Runtime cards repeat the claim rather than show execution. Fake CLI success lines imply commands were executed. Repository cards hide the actual source structure. Examples lack a guided source-to-consumer path. Benchmark bars use animated width, omit raw outliers, and normalize iOS percentiles differently from its actual harness. Layered CSS overrides add infinite blurred spectrum animations, background grids and generic reveal effects.

Replace this with a split pipeline hero, duplicated-stack comparison, real four-file project explorer, sticky compiler sequence, single runtime anatomy, tangible artifacts, example app imagery, SVG benchmark report, explicit CLI timeline and selectable repository graph. Each section answers a different question. Keep the documentation module and its existing working-tree changes intact.

The checked-in screenshots are historical: both show five requests while the current harness records eight measured rounds. Do not present their numbers as current retained evidence. Prefer current installed app captures; disclose source and snapshot limitations for every capture.

Android generate-build emits a Gradle project; Gradle assembly is a separate step. iOS generate-build archives XCFrameworks. The response decoder currently uses a bounded temporary DOM, not a claimed zero-copy direct decoder. Benchmark quantiles use floor((n-1)*q), matching both source harnesses.
