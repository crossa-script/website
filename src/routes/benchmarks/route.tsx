import { Link } from "react-router";
import { PageTransition } from "../../components/ui/PageTransition";
import { BenchmarkExplorer } from "../../features/benchmarks/BenchmarkExplorer";

export function Component(): React.JSX.Element {
  return <PageTransition className="product-page"><header className="product-intro product-wide"><p className="chapter">Engineering report / retained Release snapshots</p><h1>Measured V0<br /><span>validation.</span></h1><p>Four retained runs. Two platforms. Every measured request visible, including the outliers. Read these observations in the context of their environment.</p></header><section className="product-wide product-section evidence-report"><BenchmarkExplorer detailed /></section><section className="product-wide methodology product-section"><div><h2>Read the method.</h2><Link className="product-link" to="/docs/benchmarks">Full benchmark documentation ↗</Link></div><div><h3>Remote latency is part of the result.</h3><p>The examples request JSONPlaceholder posts. Network conditions and the emulator or simulator contribute to the duration; this does not isolate SDK overhead.</p><h3>Warm and cold are different experiments.</h3><p>Warm runs reuse clients after excluded warmups. Cold samples create fresh clients or runtimes. Measured rounds interleave implementations; configuration metadata is retained with each snapshot.</p><h3>Statistics follow the actual harness.</h3><p>p50 and p95 select the sorted successful sample at floor((n − 1) × quantile), matching both example applications. With eight samples, p95 is not the maximum. Mean includes all successful durations; raw plots preserve every measured sample and failure.</p><h3>Small runs are evidence, not a ranking.</h3><p>These are V0 Release validation observations on ARM64 emulators and simulators. They establish no physical-device speed guarantee.</p></div></section></PageTransition>;
}
