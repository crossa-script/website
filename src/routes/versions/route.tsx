import { Link } from "react-router";
import { PageTransition } from "../../components/ui/PageTransition";
import { verifiedCommit } from "../../content/product/story";
import { Visual } from "../../features/product/Visual";

export function Component(): React.JSX.Element {
  return <PageTransition className="product-page"><header className="product-intro product-wide"><p className="chapter">Release identity / documented V0 baseline</p><h1>An artifact should<br /><span>tell its own story.</span></h1><p>Trace a generated SDK back to the CLI, source revision and digest that produced it. Compiler, runtime ABI and artifact identity are separate version axes.</p></header><section className="product-wide version-story"><div><h2>Source → build → identity.</h2><dl><dt>CLI identity</dt><dd><code>crossa --version</code><p>The executable version used for validation and generation.</p></dd><dt>Verified source revision</dt><dd><code>{verifiedCommit}</code><p>The source baseline used to verify this public product story.</p></dd><dt>Generated artifact</dt><dd>Release AAR or XCFramework<p>A project-specific SDK with generated operations and native runtime.</p></dd><dt>Manifest + digest</dt><dd>SHA-256<p>Match the artifact to its recorded provenance. SwiftPM uses the framework ZIP checksum.</p></dd></dl></div><div><Visual name="android" /><Visual name="ios" /></div></section><section className="product-wide product-finish"><h2>Verify what you integrate.</h2><Link className="product-link" to="/benchmarks">Inspect retained artifact provenance ↗</Link><Link className="product-link" to="/docs/cli">Read the CLI contract ↗</Link></section></PageTransition>;
}
