import { formatMilliseconds, metricValue, type BenchmarkMetric, type BenchmarkRun } from "../../content/benchmarks/BenchmarkRepository";

export function LatencyComparisonChart({ run, metric }: { readonly run: BenchmarkRun; readonly metric: BenchmarkMetric }): React.JSX.Element {
  const max = Math.max(...run.results.map((result) => metricValue(result, metric)), 1) * 1.12;
  return <div className="latency-chart" aria-live="polite"><p className="chart-axis-title">Request latency · milliseconds · lower is shorter</p>{run.results.map((result) => { const value = metricValue(result, metric); return <div className="latency-series" key={result.implementation}><div><strong>{result.implementation}</strong><span>{formatMilliseconds(value)}</span></div><svg viewBox="0 0 800 28" preserveAspectRatio="none" role="img" aria-label={`${result.implementation}: ${formatMilliseconds(value)}, ${metric}, ${result.sampleCount} successful and ${result.failureCount} failed samples`} tabIndex={0}><title>{result.implementation}: {formatMilliseconds(value)} · {result.sampleCount} successful / {result.failureCount} failed</title><path d="M0 14H800" stroke="var(--border)" /><rect x="0" y="4" height="20" width={value / max * 800} fill={result.implementation === "Crossa" ? "var(--accent-strong)" : result.implementation === "Ktor" ? "var(--muted)" : "var(--blue)"} /></svg></div>; })}<div className="chart-scale"><span>0 ms</span><span>{formatMilliseconds(max / 2)}</span><span>{formatMilliseconds(max)}</span></div></div>;
}
